import styled from 'styled-components';
import {
    AlertDialog as ChakraDialog,
    AlertDialogBody,
    AlertDialogFooter,
    AlertDialogHeader,
    AlertDialogContent,
    AlertDialogOverlay,
} from '@chakra-ui/react';

export const StyledDialog = styled(ChakraDialog)``;
export const DialogBody = AlertDialogBody;
export const DialogFooter = AlertDialogFooter;
export const DialogHeader = AlertDialogHeader;
export const DialogContent = AlertDialogContent;
export const DialogOverlay = AlertDialogOverlay;

