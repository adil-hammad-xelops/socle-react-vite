import styled from 'styled-components';
import {Box} from '@chakra-ui/react';

// Styled wrapper for MultiSelect component if custom styling is needed
export const StyledMultiSelectWrapper = styled(Box)`
    .multi-select-tags {
        margin-top: 0.5rem;
    }

    .multi-select-button {
        width: 100%;
        text-align: left;
        font-weight: normal;
    }
`;

