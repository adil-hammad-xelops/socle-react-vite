import styled from 'styled-components';
import {
    Breadcrumb as ChakraBreadcrumb,
    BreadcrumbItem,
    BreadcrumbLink,
} from '@chakra-ui/react';

export const StyledBreadcrumb = styled(ChakraBreadcrumb)``;
export const StyledBreadcrumbItem = styled(BreadcrumbItem)``;
export const StyledBreadcrumbLink = styled(BreadcrumbLink)``;

